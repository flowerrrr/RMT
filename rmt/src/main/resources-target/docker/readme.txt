this folder contains a file credentials.properties which is not under git control.

Environment-Target: Docker
Identisch mit prod, bis auf
- jdbc URL zeigt auf hostname mysql, der vom Dockerimage verwendet wird.
- smpt-server = localhost